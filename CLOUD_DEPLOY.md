# 金成峰司机助手 · 腾讯云托管部署指南

> 适用后端：Flask + SQLite（`driver_miniprogram/server/`）
> 部署目标：**腾讯云 CloudBase「云托管」（容器服务）**

---

## 0. 为什么用 CloudBase 云托管（而不是微信云开发）

| 方案 | 是否可用 | 原因 |
|---|---|---|
| 微信小程序云开发 / 云托管 | ❌ 不可用 | 官方明确：**企业微信小程序暂不支持云开发、自动预览、真机调试**（见微信开发者工具文档「企业微信小程序·工具支持」） |
| **腾讯云 CloudBase 云托管** | ✅ **推荐** | 独立容器托管，不依赖小程序类型；自带 HTTPS 域名，企业微信小程序把域名配进「服务器域名」即可调用，无需备案域名 |
| 自建服务器 + 备案域名 | 可用 | 成本高、维护重，非首选 |

本仓库已按 CloudBase 云托管做好容器化适配：

- `Dockerfile`：python:3.12-slim + gunicorn，端口 **8081**
- `server/requirements.txt`：flask / openpyxl / gunicorn
- 配置全部支持 **环境变量注入**（密钥不进镜像）
- 数据目录可指向 **挂载卷**（`DB_PATH` / `UPLOAD_DIR`），容器重启数据不丢
- 在线表格同步在容器中自动关闭（wecom-cli 依赖本机企业微信授权，见 §7）

---

## 1. 前置条件

| 项 | 说明 |
|---|---|
| 腾讯云账号 | 已实名，开通 CloudBase（云开发/云托管） |
| 企业微信管理员 | 有权限在小程序后台配置「服务器域名」、发布小程序 |
| corpid / secret | 企业微信管理后台 → 应用管理 → 自建 → 小程序 → 开发管理，获取 企业ID 与 小程序Secret |

---

## 2. 开通 CloudBase 并创建环境

1. 打开 [腾讯云 CloudBase 控制台](https://console.cloud.tencent.com/tcb)（云开发 CloudBase）
2. 「环境」→「新建环境」：按量付费即可（月租版也可）
3. 进入环境后，左侧菜单找到 **「云托管」**（旧称 CloudRun，若入口叫「云托管服务」亦同）
4. 若提示开通，按引导开通云托管服务（会自动创建用于托管的资源）

> 说明：CloudBase 的「云托管」与本仓库的本地 `webhook_server.py`（方案 A，FRP 内网穿透）无关，二者互不影响，可并存。

---

## 3. 创建服务并部署

### 3.1 新建服务

云托管 → 「新建服务」，填写：

- **服务名称**：`driver-api`（示例）
- **构建方式**：选择 **「上传代码包（自动构建）」**（本机无需安装 Docker）
- **代码包**：把 `driver_miniprogram/` 整个目录打成 zip 上传（Dockerfile 在项目根，构建系统会自动识别）
  - 打包前确保按 §3.3 检查 `.dockerignore` 生效（driver.db 等不入包）
- **部署方式**：自动部署（代码上传后立即构建）

### 3.2 服务配置（关键，务必按此设置）

| 配置项 | 值 | 说明 |
|---|---|---|
| 端口 | `8081` | 与 Dockerfile EXPOSE 一致 |
| 实例数 | **固定 1** | SQLite 文件库不支持多实例并发写，必须单实例 |
| 内存/规格 | 0.25~0.5 核 / 512MB 即可 | 车队规模足够 |
| 公网访问 | 开启 | 自动分配 HTTPS 域名 |
| 请求超时 | 60s | 与 gunicorn --timeout 对齐 |

### 3.3 环境变量（在服务「配置管理」中设置，替代 config.json）

| 变量 | 必填 | 示例 | 说明 |
|---|---|---|---|
| `WX_CORPID` | ✅ | `ww1234abcd...` | 企业 ID，**不要写进 config.json** |
| `WX_SECRET` | ✅ | `AbCdEf...` | 企业微信小程序 secret |
| `WX_DEBUG_MODE` | ✅ | `0` | **必须 0**（正式环境走 jscode2session 真实登录） |
| `WX_ADMIN_USERIDS` | 建议 | `ZhangSan,LiSi` | 固定管理员 userid（逗号分隔） |
| `WX_WEB_PASSWORD` | ✅ | `改成强密码` | 网页端报表访问密码 |
| `WX_SYNC_ENABLED` | 建议 | `0` | 容器环境关闭在线表格同步（默认已由 Dockerfile 置 0） |
| `WX_UPLOAD_MAX_MB` | 可选 | `10` | 保养照片大小上限 |

> config.json 可保留在代码里，**环境变量优先级更高**，密钥最终以环境变量为准。

### 3.4 数据持久化（挂载 CFS，防容器重建丢数据）

容器是**无状态**的，`driver.db` 和 `uploads/` 必须放挂载卷，否则重建实例/发版时数据全丢。

1. 云托管服务 →「配置管理」→「挂载（存储）」→ 新增挂载
2. 选择或新建 **CFS 文件存储**（腾讯云文件存储，按量付费）
3. 挂载到容器路径 **`/data`**（Dockerfile 默认数据目录）

挂载后效果：

```
/data/driver.db     ← SQLite 数据库（持久化）
/data/uploads/      ← 保养照片（持久化）
```

> 💡 首次部署后即可通过网页端验证数据是否落盘：提交一条记录 → 控制台重建服务 → 数据仍在 = 挂载生效。

---

## 4. 获取 HTTPS 域名并验证

1. 服务部署完成后，在服务详情页复制 **默认 HTTPS 域名**（形如 `https://driver-api-xxxx-xxxx.ap-shanghai.run.tcloudbase.com`）
2. 浏览器验证（无需登录的接口）：
   - `https://<域名>/health` → 应返回 `ok`
   - `https://<域名>/web` → 应看到登录页（密码 = WX_WEB_PASSWORD）
3. 小程序端验证登录：小程序发 `wx.qy.login` 拿 code → POST `/api/login` 应返回 token 与真实姓名

> 云托管域名自带 HTTPS 证书，**无需再备案域名**即可用于小程序合法域名配置。

---

## 5. 企业微信小程序后台配置合法域名

真机（含体验版）上 `wx.request` / `wx.uploadFile` / `wx.downloadFile` 会校验合法域名，必须在企业微信管理后台配置：

1. 企业微信管理后台 → **应用管理** → 自建应用 → 你的小程序 → **开发管理 → 开发设置**
2. 在「服务器域名」中填写（都填云托管域名）：

| 类型 | 域名 |
|---|---|
| request 合法域名 | `https://driver-api-xxxx.ap-shanghai.run.tcloudbase.com` |
| uploadFile 合法域名 | 同上 |
| downloadFile 合法域名 | 同上（Excel 导出、照片查看用） |

3. 保存后约 1 分钟生效

> ⚠️ 域名必须 **不带路径、不带 http:// 前缀**，直接填 `https://主机名`。

---

## 6. 修改小程序端后端地址并发布

1. 修改 `miniprogram/utils/api.js` 顶部：

```js
// 正式环境：云托管 HTTPS 域名（从服务详情页复制）
const BASE_URL = 'https://driver-api-xxxx.ap-shanghai.run.tcloudbase.com'
```

2. 微信开发者工具 → 重新编译，检查登录/出车/加油/保养/报表全流程
3. 开发者工具 → 上传版本 → 企业微信管理后台 → 版本管理 → 提交审核/发布体验版
4. 首次发布后，**请用网页端填自己的 userid 登录激活首个管理员**，或用 `WX_ADMIN_USERIDS` 固定管理员

---

## 7. 功能差异说明（云托管 vs 本机）

| 功能 | 本机 | 云托管 | 说明 |
|---|---|---|---|
| 出车/加油/保养提交 | ✅ | ✅ | |
| 三类报表 + 分组汇总 + Excel 导出 | ✅ | ✅ | |
| 网页端报表（/web） | ✅ | ✅ | 用云托管域名直接访问 |
| 企业微信账号登录（真实姓名） | ✅ | ✅ | 需配 corpid/secret，关 debug_mode |
| **在线表格自动同步** | ✅ | ❌ | wecom-cli 依赖本机企业微信授权，容器无法运行；如需同步，可在本机另跑 `sync_sheet.py` 或保留本机服务 |
| 保养照片上传 | ✅ | ✅ | 存挂载卷 /data/uploads |

---

## 8. 本地数据迁移到云端（可选）

若本机 `driver.db` 已有真实数据，部署完成后把数据库文件上传到挂载卷：

1. 先在挂载卷建立目录，例如上传到 CFS 的 `/data/driver.db`
2. 用 CFS 控制台「文件管理」或服务器挂载 CFS 后直接覆盖 `/data/driver.db`
3. 注意：**先停云托管服务**再覆盖，避免进程持有旧文件句柄
4. 启动服务后访问 `/web` 验证数据

---

## 9. 常见问题

**Q1：为什么实例数必须 = 1？**
SQLite 是文件型数据库，多实例并发写同一文件会报 `database is locked`。云托管默认可以多实例，请务必固定 1 个实例。若以后人数大涨，再考虑迁 MySQL。

**Q2：容器重建后数据丢了？**
说明挂载没生效。检查挂载路径是否为 `/data`，且 `DB_PATH`/`UPLOAD_DIR` 未被人为改回默认值。

**Q3：小程序真机请求报「不在以下 request 合法域名列表中」？**
合法域名没配或没生效：确认 §5 三处域名都填了、保存后等待 1 分钟、域名无路径前缀。

**Q4：登录失败 / 拿不到真实姓名？**
确认 `WX_DEBUG_MODE=0`、`WX_CORPID`/`WX_SECRET` 正确（可在服务日志看 access_token 获取是否成功）。

**Q5：在线表格同步接口显示未启用？**
正常现象，见 §7。云托管不支持 wecom-cli。

**Q6：Excel 导出/照片查看失败？**
确认 downloadFile 合法域名已配置。

**Q7：更新代码如何发版？**
重新打 zip 上传 → 云托管自动构建新版本 → 确认无误后「流量全量切到新版本」（或开启自动发布）。

---

## 10. 成本参考

- CloudBase 按量付费：云托管计费 ≈ CPU×时长 + 内存×时长 + 流量费；车队规模（几十人、每天几十条记录）通常 **几元~几十元/月**
- CFS 文件存储：按容量与读写量计费，极小
- 另有 CloudBase 包月套餐（约 ¥19.9/月起）可覆盖大多数场景，以控制台实时价格为准
